version: '3.8'

services:
  app:
    build: .
    container_name: llmesh-api
    restart: unless-stopped
    ports:
      - "8087:8087"
    environment:
      - ENV=production
      - DOMAIN=${DOMAIN:-localhost}
      - API_BASE_URL=${API_BASE_URL:-http://localhost:8087}
      - CHAT_URL=${CHAT_URL:-http://localhost:8087}
      - SESSION_SECRET_KEY=${SESSION_SECRET_KEY}
      - ADMIN_USERNAME=${ADMIN_USERNAME:-admin}
      - ADMIN_PASSWORD_HASH=${ADMIN_PASSWORD_HASH}
      - DEFAULT_LIMIT=${DEFAULT_LIMIT:-1000000}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_BASE_URL=${OPENROUTER_BASE_URL:-https://openrouter.ai/api/v1}
      - PORT=8087
      - HOST=0.0.0.0
    volumes:
      - ./app/database:/app/app/database
      - ./logs:/app/logs
      - ./.env:/app/.env:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8087/')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - llmesh-network

  # Optional: use Nginx as a reverse proxy
  nginx:
    image: nginx:alpine
    container_name: llmesh-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - app
    networks:
      - llmesh-network
    profiles:
      - with-nginx

networks:
  llmesh-network:
    driver: bridge